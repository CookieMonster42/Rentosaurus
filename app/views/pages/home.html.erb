<div class="banner">
<div class="search">
  <h4 class="mb-4">Book unique Dinosaur around the world</h4>
  <p>Where:</p>
<%= form_tag dinosaurs_path, method: :get do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Berlin"
  %>

  <%= submit_tag "Search", class: "btn mt-3 rounded-lg" %>
<% end %>
</div>
</div>
